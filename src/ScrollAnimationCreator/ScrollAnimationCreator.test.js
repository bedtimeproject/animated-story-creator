import { render } from "@testing-library/react";
import ScrollAnimationCreator from "./ScrollAnimationCreator";

test("renders ScrollAnimationCreator", () => {
  render(<ScrollAnimationCreator />);
});
